#! /bin/bash
file=$1
org_code=$2
OLDIFS=$IFS
IFS=","
while read org1 seq1 org2 seq2
do
	if [[ "$org2" == *"$org_code"* ]]
	then
		#echo $org >> out.txt
		echo $org1,$seq1,$org2,$seq2 >> a.csv
	fi
done < "$file"
IFS=$OLDIFS

